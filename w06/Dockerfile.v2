FROM python:3.12-slim
WORKDIR /app

# 先複製相依清單，單獨裝套件（這層只在 requirements.txt 變動時才重跑）
COPY app/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 再複製應用程式碼（天天變動的放在最下面）
COPY app/ .

EXPOSE 80
CMD ["python", "app.py"]
